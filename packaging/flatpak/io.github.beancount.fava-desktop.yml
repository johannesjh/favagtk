app-id: io.github.beancount.fava-desktop
runtime: org.gnome.Platform
runtime-version: '3.38'
sdk: org.gnome.Sdk
command: fava-desktop
finish-args:
  - --share=network
  - - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=home:ro
modules:
  - name: fava-desktop
    buildsystem: simple
    build-commands:
      - pip3 install --find-links=./dist --prefix=${FLATPAK_DEST} fava fava_desktop
    build-options:
        build-args:
          - --share=network
    sources:
      - type: dir
        path: ../..
